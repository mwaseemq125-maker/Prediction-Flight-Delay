# Prediction-Flight-Delay
typically focus on building machine learning pipelines to analyze historical flight data and forecast whether a flight will be delayed (binary classification) or the specific duration of the delay (regression).
